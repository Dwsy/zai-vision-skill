{
  "name": "skill-zai-vision",
  "version": "1.0.0",
  "description": "Claude Skill wrapper for zai-vision MCP server (stdio)",
  "scripts": {
    "setup": "uv sync",
    "run": "uv run executor.py"
  }
}